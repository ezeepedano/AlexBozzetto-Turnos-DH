<div class="header-top">
    <a class="logo" href="/"
        ><img src="/images/baked.png" alt="Logo Amarta"
    /></a>
    
</div>

<nav class="navbar navbar-expand-md navbar-light">
    <div class="container-fluid">
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item"><a class="nav-link active" href="/">Inicio</a></li>
                
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <% if (!user) { %>

                <li class="nav-item">
                    <a class="nav-link active" href="/auth/login"
                        ><i class="fas fa-sign-in-alt"></i>Ingresá para Asignar un Nuevo Turno</a
                    >
                </li>

                <% } else { %>
                <li class="class="nav-item welcome-message">
                    <i class="nav-link active fas fa-user"></i> ¡Bienvenido, <%= user ?
                    user.name : "invitado" %>!
                </li>
               
                </li class="nav-item">
                <li class="nav-item">
                    <a class="nav-link active" href="/auth/logout"
                        ><i class="fas fa-sign-out-alt"></i> Salir</a
                    >
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
